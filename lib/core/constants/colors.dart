import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // prevent instance

  static const Color background = Color(0xFFFEFEFE);
  static const Color dark = Color(0xFF0E2427);
  static const Color button = Color(0xFF80B6BC);
  static const Color lightGrey = Color(0xFFF0F5F5);
  static const Color text = Color(0xFF41505C);
  static const Color mainText = Color(0xFF273846);
  static const Color seeAll = Color(0xFF96C2C7);

  // Optional base colors
  static const Color primary = button;
  static const Color secondary = dark;
  static const Color unselected = lightGrey;
}
